using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelSpawnScript : MonoBehaviour
{
    /*
    public int offsetX = 2; //offset to prevent weird errors

    private float spriteWidth = 0f; // the width of our element
    private Camera cam;
    private Transform currentLevel;

    public float levelWidth;
    public float levelHeight;

    private void Awake()
    {
        cam = Camera.main;
        currentLevel = transform;
    }
    // Start is called before the first frame update
    void Start()
    {
        Transform levelSize = transform.Find("LevelSize");
        levelWidth = levelSize.localScale.x;
        levelHeight = levelSize.localScale.y;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
